{
  "name": "full-content-inventory",
  "private": true,
  "version": "0.1.0",
  "description": "Full content inventory CLI - crawl websites and generate Google Drive content inventories",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev": "pnpm -r dev"
  }
}
